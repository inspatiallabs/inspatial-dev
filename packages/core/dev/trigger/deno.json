{
  "name": "@inspatial/trigger",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "homepage": "https://www.inspatial.dev",
  "repository": "https://github.com/InSpatiallabs/inspatial-dev",
  "author": "InSpatial Labs",
  "bugs": "https://github.com/InSpatiallabs/inspatial-dev/issues",
  "description": "InSpatial Trigger is a universal trigger (event listener) bridge system that works seamlessly across different platforms (Web, Native & 3D environments). It allows events to propagate between platforms with automatic mapping and transformation.",
  "keywords": [
    "trigger",
    "trigger system",
    "trigger bridge",
    "trigger event",
    "trigger registry",
    "event",
    "event handler",
    "event bridge",
    "event listener",
    "event handler",
    "event bridge"
  ],
  "tasks": {
    "test": "deno test --allow-env --allow-read --allow-write --allow-run --allow-net --allow-hrtime --allow-env --allow-all ./src/index.test.ts"
  },
  "imports": {
    "#test": "../packages/core/dev/test/src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "deno.ns", "deno.window"]
  }
}
